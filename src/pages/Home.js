import React from 'react';
import Logo from '../components/Logo';
import Nav from '../components/Nav';

const Home = () => {
    return (
        <div>
            <Logo />
            <Nav />
            <h1>Accueil</h1>
        </div>
    );
};

export default Home;